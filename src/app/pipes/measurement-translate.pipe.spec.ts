import { MeasurementTranslatePipe } from './measurement-translate.pipe';

describe('MeasurementTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new MeasurementTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
